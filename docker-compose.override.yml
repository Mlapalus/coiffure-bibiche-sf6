version: '3'

services:
###> doctrine/doctrine-bundle ###
  database:
    ports:
      - "5432"
    environment:
      POSTGRES_DB: coiffure
      # You should definitely change the password in production
      POSTGRES_PASSWORD: root
      POSTGRES_USER: root
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
  mailer:
    image: schickling/mailcatcher
    ports: ["1025", "1080"]
###< symfony/mailer ###
